const nomesFake = [ 
    "Carlos Silva", "Ana Souza", "Luis Filipe", "Rafael", "Luigi", "Felipe Lima"
];

function gerarNomeFake() {
    const indice = Math.floor(Math.random() * nomesFake.length); 
    return nomesFake[indice];
}

module.exports = { gerarNomeFake };
